var formulas = (function() {
	return {
		formula1 : "\\log_2 N",
		formula2 : "\\(x, y) + (z, t) = (x + z, y + t)",
		formula3 : "\\(x, y) (z, t) = (xz - yt, xy + yz)"
	};
})();

var graphic = (function() {
	
})
